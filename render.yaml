services:
  - type: web
    name: vector-ai-academy-site
    runtime: static
    rootDir: course_landing/landing_page
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
